---
title: "16. 미분방정식과 동역학"
---
# 16강. 미분방정식과 동역학

이 강의의 목표는 현재 상태의 변화율로 시간에 따른 움직임을 읽는 법을 아주 기초부터 이해하는 것입니다.

먼저 오늘의 핵심 식을 봅니다.

$$
\frac{dx}{dt}=f(x,t)
$$

이 식을 외우기 전에, 식 안의 말과 기호를 먼저 하나씩 풀어야 합니다.

# 1. 먼저 오늘 쓸 말을 정리하자

이번 강에서 새로 등장하는 어휘만 모았습니다. 미분·도함수(12강), 적분·원시함수(14강), 지수함수 $e^x$(4강)는 앞서 본 그대로 씁니다.

| 말 | 뜻 |
|---|---|
| 상태 $x$ | 지금 시스템이 어떤 모습인지 나타내는 값 |
| 시간 $t$ | 변화가 진행되는 기준 |
| 변화율 $dx/dt$ | 상태가 얼마나 빠르게 바뀌는지 나타내는 값 |
| 미분방정식 | 상태와 변화율 사이의 관계식 |
| 초기조건 | 처음 상태를 알려 주는 값 $x(0) = x_0$ |
| 해 | 미분방정식을 만족하는 상태 함수 $x(t)$ |
| 변수분리 | $x$항과 $t$항을 양변으로 갈라 적분하는 풀이법 |
| 수치해법 | 해를 식으로 못 풀 때 조금씩 전진하며 근사하는 방법 |

수학에서 어려운 부분은 계산보다 읽기입니다. 뜻을 모르고 계산하면 공식이 암호처럼 보입니다.

# 2. 먼저 떠올릴 장면

공의 현재 위치와 속도를 알면 잠시 뒤 위치를 예측할 수 있습니다. 미분방정식은 지금 상태가 다음 순간 어떻게 변할지 적는 언어입니다.

# 3. 핵심 식을 천천히 읽어 보자

- $x$는 시간에 따라 변하는 상태입니다.
- 왼쪽 $dx/dt$는 $x$가 시간에 따라 변하는 속도입니다.
- 오른쪽 $f(x,t)$는 그 변화율을 정하는 규칙입니다.
- 초기조건이 있어야 실제 움직임 하나가 정해집니다.

즉 핵심 식은 단순히 기호를 나열한 것이 아니라, 무엇을 보고 무엇을 계산할지 알려 주는 문장입니다. 일반 방정식은 "어떤 수 $x$인가"를 묻지만, 미분방정식은 "어떤 함수 $x(t)$인가"를 묻습니다.

# 4. 가장 기본이 되는 방정식: $dx/dt = kx$

자연에서 가장 자주 나오는 미분방정식은 "변화율이 현재값에 비례한다"는 것입니다.

$$
\frac{dx}{dt} = k x
$$

인구 증가, 이자, 방사성 붕괴, 냉각처럼 지금 양이 많을수록 더 빨리(또는 더 빨리 줄어드는) 상황이 모두 이 형태입니다. 이 식을 **변수분리**로 단계별로 풀어 봅니다.

**1단계. $x$항과 $t$항을 양변으로 가릅니다.** 양변을 $x$로 나누고 $dt$를 곱합니다($x \ne 0$ 가정).

$$
\frac{1}{x}\, dx = k \, dt
$$

**2단계. 양변을 적분합니다.** 왼쪽은 $\dfrac{1}{x}$의 적분, 오른쪽은 상수 $k$의 적분입니다(14강 부정적분표).

$$
\int \frac{1}{x}\, dx = \int k \, dt
\quad\Rightarrow\quad
\ln|x| = k t + C
$$

**3단계. 지수를 씌워 $x$를 풉니다.** 양변을 $e$의 지수로 올리면

$$
|x| = e^{kt + C} = e^{C} \cdot e^{kt}
$$

$e^C$는 그냥 어떤 양의 상수이므로 부호까지 합쳐 $A = \pm e^C$로 두면

$$
x(t) = A\, e^{kt}
$$

**4단계. 초기조건으로 상수를 정합니다.** $t = 0$에서 상태가 $x_0$라면, $x(0) = A e^{0} = A = x_0$. 따라서

$$
\boxed{\,x(t) = x_0\, e^{kt}\,}
$$

변화율이 현재값에 비례하면 상태는 **지수함수**로 움직입니다. $k > 0$이면 지수 증가, $k < 0$이면 지수 감쇠입니다.

# 5. 예제 1: 지수감쇠 $dx/dt = -x$

문제: $\dfrac{dx}{dt} = -x$이고 초기조건이 $x(0) = 5$일 때 해를 구하고 곡선을 설명하시오.

풀이:

4절에서 $k = -1$인 경우입니다. 공식에 그대로 넣으면

$$
x(t) = x_0\, e^{-t} = 5\, e^{-t}
$$

곡선의 모양은 다음과 같습니다.

- $t = 0$에서 $x = 5$로 시작합니다.
- 시간이 지날수록 값이 점점 줄지만, 줄어드는 속도도 함께 느려집니다($dx/dt = -x$이므로 $x$가 작아지면 변화율도 작아집니다).
- $t \to \infty$에서 $x \to 0$에 한없이 가까워지지만 정확히 0에 닿지는 않습니다.

몇 개 값을 표로 보면 감쇠가 분명해집니다.

| $t$ | $x(t) = 5e^{-t}$ |
|---|---|
| 0 | 5.000 |
| 1 | 1.839 |
| 2 | 0.677 |
| 3 | 0.249 |

매 단위 시간마다 같은 비율($e^{-1} \approx 0.368$)로 줄어드는 것이 지수감쇠의 특징입니다.

# 6. 예제 2: 지수증가 $dx/dt = 0.5\,x$

문제: $\dfrac{dx}{dt} = 0.5\, x$이고 $x(0) = 100$일 때 $t = 2$에서의 값을 구하시오.

풀이:

$k = 0.5$이므로 해는

$$
x(t) = 100\, e^{0.5 t}
$$

$t = 2$를 넣으면

$$
x(2) = 100\, e^{1} = 100 \times 2.71828 = 271.83
$$

변화율이 양의 비례라서 상태가 점점 더 빠르게 커집니다. 이것이 복리·인구 폭발의 수학적 모습입니다.

# 7. 오일러 방법: 식으로 못 풀 때 조금씩 전진하기

$f(x, t)$가 복잡하면 4절처럼 깔끔한 식으로 풀리지 않습니다. 이때는 **수치해법**으로 조금씩 전진하며 해를 근사합니다. 가장 단순한 것이 **오일러(Euler) 방법**입니다.

아이디어는 12강의 1차 근사입니다. 짧은 시간 $\Delta t$ 동안은 변화율이 거의 일정하다고 보고, 접선을 따라 한 걸음 나아갑니다.

$$
x_{n+1} = x_n + f(x_n, t_n)\, \Delta t
$$

- $x_n$: $n$번째 시각 $t_n$에서의 상태 추정값
- $f(x_n, t_n)$: 그 자리에서의 변화율(접선 기울기)
- $\Delta t$: 한 걸음의 시간 폭(스텝 크기)

## 7.1 수치 예: $dx/dt = -x$를 손으로 굴려 보기

5절의 정답($x(t) = 5e^{-t}$)이 있는 문제를 오일러 방법으로 근사해, 근사가 얼마나 맞는지 봅니다. $x_0 = 5$, $\Delta t = 0.5$, $f(x) = -x$로 두 걸음 전진합니다.

**첫 걸음** ($t_0 = 0 \to t_1 = 0.5$):

$$
x_1 = x_0 + f(x_0)\,\Delta t = 5 + (-5)(0.5) = 2.5
$$

**둘째 걸음** ($t_1 = 0.5 \to t_2 = 1.0$):

$$
x_2 = x_1 + f(x_1)\,\Delta t = 2.5 + (-2.5)(0.5) = 1.25
$$

$t = 1$에서 근사값은 $1.25$입니다. 정확한 해 $5e^{-1} = 1.839$와는 차이가 있습니다. $\Delta t$가 클수록 오차가 커지고, $\Delta t$를 잘게 줄일수록 참값에 가까워집니다. 계산을 많이 하는 대가로 정확도를 얻는 것이 수치해법의 기본 거래입니다.

# 8. 계산 순서로 다시 보기

1. 상태 $x$를 정합니다.
2. 상태가 어떻게 변하는지 변화율 식 $dx/dt = f(x,t)$를 씁니다.
3. 처음 상태 $x(0) = x_0$를 정합니다.
4. 풀 수 있으면 변수분리·적분으로 해 $x(t)$를 구합니다.
5. 못 풀면 오일러 방법으로 짧은 걸음을 반복해 근사합니다.
6. 이 과정을 반복해 전체 움직임을 봅니다.

이 순서를 말로 설명할 수 있으면, 공식을 완전히 외우지 않아도 다시 만들어 낼 수 있습니다.

# 9. AI에서 왜 중요한가

미분방정식은 최근 딥러닝에서 모델 자체를 정의하는 언어로 쓰입니다.

## 9.1 Neural ODE: 층 대신 미분방정식

보통 신경망은 층을 하나씩 쌓아 상태를 바꿉니다.

$$
h_{n+1} = h_n + g(h_n, \theta)
$$

이 식을 잘 보면 7절의 오일러 한 걸음과 똑같은 모양입니다($\Delta t = 1$인 오일러 스텝). 여기서 걸음 폭을 무한히 잘게 줄이면 층의 쌓임이 연속적인 미분방정식이 됩니다.

$$
\frac{dh}{dt} = g(h, t, \theta)
$$

이렇게 은닉상태의 변화를 미분방정식으로 정의한 모델이 **Neural ODE**입니다. 층 수를 고정하는 대신, 검증된 수치 ODE 솔버가 필요한 만큼 걸음을 잡아 상태를 전진시킵니다.

## 9.2 연속시간 모델

- **연속 정규화 흐름(CNF)**: 확률분포를 시간에 따라 흐르게 해 복잡한 분포를 만듭니다.
- **확산모델(Diffusion, 36강 예고)**: 데이터에 잡음을 조금씩 더하고 되돌리는 과정을 미분방정식(SDE/ODE)으로 기술합니다.
- **불규칙 시계열**: 관측 간격이 들쭉날쭉한 데이터를, 연속시간 상태로 두고 필요한 시각만 적분해 다룹니다.

> 한 줄로: 신경망의 층 쌓기를 무한히 잘게 만들면 미분방정식이 되고, 그것을 푸는 일이 곧 모델을 실행하는 일이다.

# 10. 한 줄씩 다시 요약하기

- 오늘 배운 핵심은 현재 상태의 변화율로 시간에 따른 움직임을 읽는 법입니다.
- 미분방정식은 "어떤 수"가 아니라 "어떤 함수 $x(t)$"를 묻습니다.
- $dx/dt = kx$는 변수분리·적분으로 풀면 $x(t) = x_0 e^{kt}$가 됩니다.
- $k<0$이면 지수감쇠, $k>0$이면 지수증가입니다.
- 초기조건 $x(0)=x_0$가 있어야 해가 하나로 정해집니다.
- 식으로 못 풀면 오일러 방법 $x_{n+1} = x_n + f(x_n,t_n)\Delta t$로 근사합니다.
- AI에서는 Neural ODE와 확산모델 같은 연속시간 모델이 미분방정식으로 정의됩니다.
- 핵심 식은 다음과 같습니다.

$$
\frac{dx}{dt}=f(x,t)
$$

# 11. 스스로 점검

1. 미분방정식은 일반 방정식과 무엇이 다른가?
2. 초기조건은 왜 필요한가?
3. $dx/dt = kx$를 변수분리로 풀어 $x(t) = x_0 e^{kt}$를 유도할 수 있는가?
4. $dx/dt = -2x$, $x(0) = 3$의 해를 쓸 수 있는가?
5. 지수감쇠 곡선이 왜 0에 닿지 않고 가까워지기만 하는지 설명할 수 있는가?
6. 오일러 방법의 한 걸음 식이 12강의 1차 근사와 어떻게 같은지 말할 수 있는가?
7. $\Delta t$를 줄이면 왜 오일러 근사가 더 정확해지는가?
8. Neural ODE가 왜 오일러 스텝을 무한히 잘게 만든 것과 같은지 설명할 수 있는가?
