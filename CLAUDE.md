# CLAUDE.md — do4ai 위키 작업 규칙

이 레포(`vault/`)는 WikiJS(`wiki.do4ai.com`)로 발행되는 운영 위키의 정본이다. 문서를 만들거나 고칠 때 아래를 지킨다.

## 1. 문서 말투 (가장 중요)

위키 문서·강의 자료는 **일관된 가이드/설명체**로 쓴다. 강의 자료처럼 차분한 설명 문장이 기준이다.

- **평서체(한다체)를 기본**으로 한다: `~한다 / ~이다 / ~본다 / ~하면 된다`. 단, 같은 섹션·기존 문서가 `합니다체`면 그 어미에 맞춰 일관되게 쓴다(한 문서 안에서 혼용 금지).
- **AI 어시스턴트식 말투를 쓰지 않는다.** 아래는 금지:
  - 1인칭/대행: "제가", "도와드리겠습니다", "만들어 드릴게요"
  - 독자에게 질문/권유: "~할까요?", "원하시면 ~", "필요하시면 ~", "다음으로 ~할까요"
  - 영업·하대 섞인 존대: "~하시면 됩니다", "~보시면 됩니다", "오셨다면"
  - 군더더기 이모지 장식, 과장("아주 잘", "완벽히"), 메타 코멘트("정리하면", "요약하자면 ~해 드렸습니다")
- 독자를 부르지 말고 **대상을 서술**한다. (×) "원하면 Grafana를 보세요" → (○) "Grafana에서 5xx율을 본다."
- 경고·주의는 `⚠️` 또는 `주의:` 같은 일관된 마커로, 장식이 아니라 정보로 쓴다.

## 2. IA / 구조

- 최상위: **`시작하기`(신입 온보딩 입구)** · **`Guide`(이해)** · **`Manual`(도구 사용법)** · Domains · Convention · Wiki · Lecture.
- **Guide = 서비스·개념**: `01. 인프라와 플랫폼`(기반) / `02. 서비스 운영`(서비스별) / `03. 관측과 SRE`(관측 솔루션·서비스별 조치·SLO·인시던트).
- **Manual = 도구 사용 절차**(ArgoCD·k3s·Grafana·Kibana·Tempo·Infisical·Harbor·Alerta).
- 서비스별 운영 절차는 Guide의 서비스 폴더(`3. 운영 절차`)에, 도구 사용법은 Manual에 둔다.

## 3. 넘버링

- **1부터 빈칸 없이 연속**, **두 자리 zero-pad**(`01, 02, … 10`). 한 자리면 WikiJS 트리(문자열 정렬)에서 `10`이 `2`보다 앞에 온다.
- 같은 레벨은 모두 `NN. 제목/index.md`(폴더+인덱스)로 통일한다. 폴더와 단일 파일을 같은 레벨에 섞지 않는다.
- 항목 삽입·삭제 시 뒤 번호를 당기거나 밀고, **폴더명·frontmatter title·H1·문서 간 링크·Page Tree**를 함께 갱신한다.

## 4. 정확성

- 인프라·서비스·솔루션 설명은 **실제 소스와 대조**해 쓴다(루트 `gitops/` 서브모듈 매니페스트, 각 서비스 레포). 버전·접속 URL·연결관계는 추정하지 말고 확인한다.
- 미확정 값은 `TODO(확정 필요)`로 표시한다. 평문 시크릿·SPOF 등 개선 과제는 `⚠️`로 남긴다.

## 5. 발행·검증

- 발행 경로: `vault/`(GitHub) → 클러스터 `content-sync` 사이드카(60초 주기 `main` pull) → WikiJS. **`main`에 머지하면 운영에 반영**된다.
- 문서 변경 후 검증:
  - 링크: 상대 링크 깨짐 0 (스크립트로 점검).
  - 발행 미리보기: `python3 scripts/wiki_wikijs_sync.py sync --dry-run`.
- 루트 `vault/index.md`는 발행되지 않는 중복 허브, 워크스페이스 홈 `vault/SenseCore AI LAB, AI융합학부/index.md`가 `/home`으로 발행된다. 둘의 Page Tree를 함께 맞춘다.

## 6. 인프라/배포

- 인프라·GitOps·배포·롤백 작업은 루트 `gitops/` 서브모듈 매니페스트와 운영 문서를 함께 참고한다(`AGENTS.md`).
