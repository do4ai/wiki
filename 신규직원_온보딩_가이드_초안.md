# 신규 직원 온보딩 가이드 (초안)

> **이 문서의 목적**
> SCAI LAB(데이터센스)의 개발/운영 인프라·CI/CD를 처음 접하는 신입이 **이 한 문서를 입구로 삼아** 기존 위키(Guide 01~03)를 막힘 없이 따라갈 수 있게 하는 "지도 + 첫 주 동선" 문서다.
> 기존 위키는 **도구별·서비스별 레퍼런스**로는 충실하지만, "처음 들어온 사람이 무엇부터 보고 무엇을 해야 하는지"의 **학습 경로(onboarding path)**가 빠져 있다. 이 문서가 그 경로를 채운다.
>
> **작성/반영 주의**: 위키 vault는 Notion에서 자동 생성된다(`wiki/README.md` 참고). 따라서 이 내용을 실제 위키에 반영할 때는 **Notion 원본**(`SenseCore AI LAB, AI융합학부/Guide`)에 추가해야 하며, `vault/` 파일을 직접 수정하면 다음 export 때 사라진다.

---

## 0. 5분 요약 — 우리 인프라 한 장 그림

```
[ 사용자/브라우저 ]
        │  HTTPS
        ▼
[ CloudFront (엣지, TLS 종료) ]              ← 공개 도메인의 viewer TLS는 여기서 끝남
        │  HTTP 80 (hidden origin)
        ▼
[ k3s 클러스터의 ingress-nginx ]            ← 클러스터 단일 진입점 (HostPort 80/443)
        │
        ├─► do4i        (agents.do4i.com, admin.do4i.com)      네임스페이스 do4i
        ├─► passv        (app/api/admin.passv.co.kr)            네임스페이스 passv  ※EC2→K8s 이전 중
        ├─► palcar       (palcar.do4ai.com)                     네임스페이스 palcar
        ├─► papersens    (papersens.do4ai.com, *.ps.do4ai.com)  네임스페이스 papersens
        └─► wiki         (wiki.do4ai.com)                       네임스페이스 atlas  ※코드상 atlas = 서비스 wiki

배포는 어떻게 도는가 (GitOps):
   개발자가 gitops 레포 main에 push
        → ArgoCD가 레포를 "원하는 상태(desired state)"로 읽고
        → 클러스터를 그 상태로 자동 수렴(reconcile)
   즉, "클러스터에서 직접 고치는 것"이 아니라 "레포를 고치면 클러스터가 따라온다".

관측/알림은 어떻게 도는가:
   메트릭(Prometheus) ┐
   로그(Filebeat→Elasticsearch→ElastAlert) ├─► Alerta(인시던트 집계) ─► Discord
   트레이스(OTel→Tempo) ┘                    (ArgoCD 배포 실패, 앱 발화 실패도 Alerta로 모임)
   대시보드: Grafana / 로그 검색: Kibana / 트레이스: Grafana Explore→Tempo
```

이 그림의 각 박스가 기존 위키 어느 문서에 대응하는지는 **5장 사이트맵**에 정리했다.

---

## 1. 접속 포인트 & 계정 (가장 먼저 북마크)

> 아래 URL/계정은 gitops 매니페스트 기준 실제 값이다. **공개 위키에는 평문 비밀번호를 적지 않는 것이 원칙**이므로, 실제 자격증명은 입사 시 담당자에게 받고 이 표에는 "어디서 받는지"만 남기는 것을 권장한다(→ 6장 보안 메모).

| 용도 | URL | 계정/접근 | 비고 |
|------|-----|-----------|------|
| 배포 상태 (ArgoCD) | `argocd.do4ai.com` | admin (초기 자격증명은 담당자에게) | 앱이 OutOfSync/Degraded인지 1순위 확인 |
| 대시보드 (Grafana) | `grafana.do4ai.com` (dev: `grafana.dev.do4ai.com`) | admin | NodePort 30300 |
| 메트릭 (Prometheus) | `prometheus.do4ai.com` (dev: `prometheus.dev.do4ai.com`) | basic auth | 원시 메트릭/쿼리 |
| 로그 (Kibana) | `kibana.do4ai.com` | admin | 인덱스 `filebeat-*`, 필터 `kubernetes.namespace:<서비스>` |
| 인시던트/알림 (Alerta) | `alerta.do4ai.com` | 인증 필요 | 알림이 여기에 모임, Discord로도 전송 |
| 클러스터 대시보드 (Headlamp) | `headlamp.do4ai.com` | 토큰/세션 | **읽기 전용**. ⚠️ 오타 호스트 `heaplamp.do4ai.com`도 등록돼 있음(정리 대상) |
| 시크릿 관리 (Infisical) | `infisical.do4ai.com` | admin@do4ai.com | 모든 앱 시크릿의 원천 |
| 이미지 레지스트리 (Harbor) | `harbor.do4i.com` | Infisical 관리 | ⚠️ 도메인이 `do4i.com`(do4ai 아님). 프로젝트 `do4ai`, 경로 `harbor.do4i.com/do4ai/*` |

**클러스터**: `do4ai-prod`(운영), `do4ai-dev`(개발). 네임스페이스는 환경 접미사 없이 서비스명만 사용한다(`do4i`, `passv`, `palcar`, `papersens`, `atlas`, `namanva`).

**입사 첫날 체크리스트**
- [ ] 위 8개 URL 접속 가능 여부 확인 (VPN/네트워크 포함)
- [ ] `kubectl` 설치 + kubeconfig 수령 + 클러스터 접근 권한 확인 (담당자 문의)
- [ ] GitHub `do4ai` org / `gitops` 레포 접근 권한
- [ ] Discord 운영 알림 채널 초대 확인
- [ ] Infisical / Harbor 계정 발급

---

## 2. 신입 학습 경로 (기존 위키 연결)

기존 위키 문서를 **순서대로 다 읽는 것**은 비효율적이다. 아래 순서로 읽으면 "개념 → 우리 환경 → 실전"으로 이어진다.

### 1주차 — "지도 펴기" (개념과 전체 구조)
1. 이 문서 0~1장 (전체 그림 + 접속 포인트)
2. `Guide/01. 인프라와 플랫폼/01. Kubernetes 기본 구조 가이드`
3. `Guide/01. 인프라와 플랫폼/03. k3s GitOps 운영 클러스터 초보자 가이드` ← **신입 핵심**
4. `Guide/01. 인프라와 플랫폼/04. GitOps 운영 모델 가이드` (레포가 기준, 클러스터는 수렴)
5. `Guide/03. 관측과 SRE/06. 모니터링·알림 아키텍처 가이드` (관측 전체 흐름 한 장)

> 1주차 목표: "배포는 gitops 레포가 기준이고 ArgoCD가 수렴시킨다", "장애 알림은 Alerta→Discord로 온다"를 말로 설명할 수 있다.

### 2주차 — 도구 손에 익히기
6. `Guide/01.../05. ArgoCD 운영 흐름 가이드` + 실제 `argocd.do4ai.com` 둘러보기
7. `Guide/01.../06. ingress-nginx`, `07. Harbor`, `08. Infisical`, `09. Headlamp`
8. `Guide/03. 관측과 SRE/02. 메트릭`, `03. 알림`, `04. 로그`, `05. 트레이싱`
9. 실습: 본인 담당(또는 임의) 서비스 하나를 골라 ArgoCD → Grafana → Kibana에서 직접 상태 확인

### 3주차 — 담당 서비스 + 첫 대응
10. `Guide/02. 서비스 운영/서비스 공통 1차 대응 절차` ← **장애 대응의 뼈대**
11. 담당 서비스의 `02. 서비스 운영/<서비스> 서비스 가이드` 3종(아키텍처·모니터링·운영절차) 정독
12. `Guide/03.../07. 서비스별 관측과 조치/<서비스> 관측과 조치`
13. `Guide/03.../10. 장애 대응 의사결정 가이드` + `09. 알림에서 인시던트, 에스컬레이션까지`
14. 과거 인시던트 1건을 Alerta에서 찾아 선배와 함께 리뷰

> ※ `Guide/03.../11, 12번`(Discord 리포트 리서치/설계)은 **운영 가이드가 아니라 설계 배경 문서**다. 신입 필독이 아니며, 알림 파이프라인의 "왜 이렇게 만들었나"가 궁금할 때만 본다.

---

## 3. 빠른 용어/도구 사전

기존 위키가 설명 없이 자주 쓰는 용어들이다.

| 용어 | 한 줄 설명 |
|------|-----------|
| **k3s** | 가벼운 Kubernetes 배포판. 우리 클러스터의 실체 |
| **GitOps** | "Git 레포가 인프라의 정답지". 레포를 바꾸면 클러스터가 따라옴 |
| **ArgoCD** | GitOps 컨트롤러. 레포(desired)와 클러스터(live)를 비교해 수렴시킴 |
| **desired / live / drift / reconcile** | 원하는 상태 / 실제 상태 / 둘의 차이 / 차이를 메우는 과정 |
| **Sync Status / Health Status** | (ArgoCD) Git과 같은가 / 리소스가 정상 동작하는가. **둘을 같이 봐야 함** |
| **OutOfSync** | Git과 클러스터가 다름. **그 자체로 장애 아님**(배포 중일 수 있음) |
| **kustomize base/overlay** | 공통 매니페스트(base) + 환경별 차이(overlay/prod·dev) |
| **Ingress** | 외부 HTTP(S) 요청을 어떤 서비스로 보낼지 정한 라우팅 규칙 |
| **Infisical** | 시크릿(비밀번호/키) 중앙 저장소. 앱은 여기서 받은 값을 K8s Secret으로 주입받음 |
| **Harbor** | 사설 컨테이너 이미지 저장소(`harbor.do4i.com`) |
| **Alerta** | 여러 곳의 알림을 모아 상태(open/ack/closed)로 관리하고 Discord로 보내는 인시던트 허브 |
| **Prometheus / Grafana** | 메트릭 수집·저장 / 대시보드 |
| **Filebeat / Elasticsearch / Kibana / ElastAlert** | 로그 수집 / 저장 / 검색 / 로그기반 알림 |
| **OpenTelemetry / Tempo** | 트레이스 수집 / 저장 (요청이 어디서 느린지 추적) |
| **SLO / SLI / 에러 버짓 / 번레이트** | 목표치 / 실측 지표 / 허용 실패량 / 그 소진 속도 |
| **CrashLoopBackOff / ImagePullBackOff / OOMKilled / Pending** | 앱 반복 죽음 / 이미지 못 받음 / 메모리 초과 종료 / 스케줄 대기 |
| **HPA / PDB / StatefulSet** | 부하 따라 replica 자동조절 / 동시 중단 최소보장 / 상태를 가진 워크로드(예: DB) |
| **상태 용어 LiveKit·Simli·Minimax·OpenRouter·Ollama** | 외부 의존성: 실시간통신·AI아바타·TTS / LLM API·로컬 LLM |

---

## 4. 시나리오별 "첫 대응" (어디부터 볼까)

알림을 받거나 장애가 의심될 때, **공통 1차 대응 절차**를 기본으로 하되 아래 분기로 빠르게 좁힌다.

### 공통 1차 (모든 상황)
```bash
# 1) ArgoCD 앱 상태
sudo kubectl get applications -A           # 또는 argocd.do4ai.com
# 2) 해당 네임스페이스 리소스
sudo kubectl get deploy,sts,svc,ing -n <namespace>
sudo kubectl get pods -n <namespace>
# 3) 로그
sudo kubectl logs deploy/<name> -n <namespace> --tail=100
```
체크 키워드: `OutOfSync`, `Degraded`, `CrashLoopBackOff`, `ImagePullBackOff`, `Pending`, `OOMKilled`

### 증상별 분기

| 증상 | 먼저 의심 | 보는 곳 |
|------|----------|---------|
| 배포했더니 앱이 안 뜸 | 직전 배포 / 이미지 / 시크릿 | ArgoCD Health → pod events → 로그. 직전 배포가 원인이면 **롤백** |
| `ImagePullBackOff` | 이미지 태그/레지스트리 권한 | overlay `images:` 태그 → Harbor에 태그 존재? → pull 시크릿 |
| `CrashLoopBackOff` | 설정/시크릿/코드 | 앱 로그, env/Secret 주입 여부(Infisical) |
| `OOMKilled` (특히 papersens) | 메모리 한계 | Grafana 메모리 → 리소스 limit 상향 검토 |
| 특정 도메인만 5xx/접속불가 | ingress/라우팅 | `kubectl describe ingress`, Grafana `nginx-ingress`, 5xx율 |
| 사용자에 영향 큰 광역 장애 | 공통 인프라 | 노드 상태/PVC/ingress, **즉시 에스컬레이션** |
| 챗봇 발화 실패(passv) | LLM/아바타 의존성 | 5xx로 안 잡힘 → Alerta `ChatGenerationFailed`, LLM/LiveKit/Simli 키 |
| 위키 문서 미갱신 | content-sync | `kubectl logs deploy/atlas -c content-sync -n atlas`(레포 pull 실패) |

### 롤백 판단 (요지)
- 최근 배포 직후 critical/에러율 급증 → **즉시 롤백**(직전 정상 리비전), 원인분석은 그 다음
- 원인 명확한 단순 버그면 핫픽스가 더 빠를 수 있음
- 인프라 신호(노드 Pressure/PVC/MySQL Ready 0)면 앱 재배포가 아니라 인프라 조치
- **prod의 do4i/passv는 ArgoCD 자동 동기화가 꺼져(게이팅) 있을 수 있음** → 롤백 시 sync 동작 방식 확인
- 자세한 판단표: `Guide/03.../10. 장애 대응 의사결정 가이드`

---

## 5. 기존 위키 사이트맵 — 현재 구조 평가 & 신입 친화 재구성 제안

### 현재 구조 (Guide)
```
Guide/
├── 01. 인프라와 플랫폼   (01 K8s ~ 09 Headlamp)   ← 도구별 레퍼런스
├── 02. 서비스 운영        (공통 1차 + 서비스 5종 × 아키/모니터링/운영)  ← 서비스별 레퍼런스
└── 03. 관측과 SRE         (01 개념 ~ 12 설계)      ← 도구 + 프로세스 + 설계 혼재
```

### 신입 관점 진단
- **장점**: 도구별·서비스별로 잘 정돈된 **레퍼런스**다. 무엇이 어디 있는지 찾기는 좋다.
- **약점 1 (학습 경로 부재)**: 01→02→…→09 순서가 "개념 5할, 실행 0할"로 흘러, 다 읽어도 "그래서 내가 뭘 하지?"가 남는다. 진입점/순서 안내가 없다.
- **약점 2 (개념과 실행의 단절)**: 01~05는 원리, 06~09는 설정값. 그 사이 "실제로 접속해서 이렇게 한다"가 비어 있다.
- **약점 3 (성격이 다른 문서 혼재)**: `03`의 11·12번은 **설계/리서치**인데 운영 가이드와 한 줄에 섞여 신입이 "이게 확정인가?"로 혼란.
- **약점 4 (미확정 TODO 다수)**: SLO 값, Discord 채널/온콜, passv 이전 시점 등이 "TODO 확정 필요"로 남아 신입이 판단 기준을 못 잡음.
- **약점 5 (Manual 링크 단절)**: 여러 문서가 "자세한 절차는 Manual"로 넘기지만 Manual이 이 트리에 연결돼 있지 않음.

### 제안 — "경로형 입구 + 기존 레퍼런스 유지"
기존 트리를 갈아엎지 말고, **맨 앞에 경로형 진입 섹션(이 문서)**을 추가하고 성격이 다른 문서만 분리한다.

```
Guide/
├── 00. 시작하기 (신입 온보딩)          ← 신규: 이 문서 (지도 + 접속 + 학습경로 + 첫 대응 + 용어)
├── 01. 인프라와 플랫폼   (그대로, 레퍼런스)
├── 02. 서비스 운영        (그대로, 레퍼런스)
├── 03. 관측과 SRE         (01~10만 운영 가이드로 유지)
└── 90. 설계·리서치 아카이브            ← 신규: 03의 11·12번을 이리로 이동(설계 배경)
```

핵심은 **레퍼런스(찾아보는 문서)와 가이드(따라 하는 문서)를 분리**하는 것이다. 신입은 `00`에서 시작해 필요할 때 01~03 레퍼런스로 내려간다.

### 동반 권장 정리 작업(별도 태스크)
- `heaplamp.do4ai.com` 오타 ingress 정리
- 공개 위키에서 평문 기본 비밀번호(`Admin12!`) 제거 → Infisical 경로 안내로 대체
- SLO/온콜/Discord 채널 등 "TODO 확정 필요" 항목에 **소유자 + 완료 예정일** 명시
- 끊긴 Manual 링크의 실존 여부 점검 및 연결
- gitops README의 시크릿 섹션(Sealed Secrets 기준)을 실제 baseline(Infisical Operator)에 맞게 갱신

---

## 부록 A. 검토 중 확인된 "사실 정확성" 메모 (가이드 vs 실제 gitops)

대체로 가이드 내용은 실제 매니페스트와 **일치**한다. 다만 아래는 신입에게 혼란을 줄 수 있어 명시한다.

- **대체로 정확**: 클러스터(do4ai-prod/dev), ingress 포트(HostPort 80/443, NodePort 30800/30843), ArgoCD 앱 트리(prod 21개 / dev 10개), Grafana/Prometheus/Kibana/Alerta/Infisical/Harbor 도메인, GitOps "main push = 배포 신호".
- **주의 표기 필요**:
  - Harbor 도메인은 `harbor.do4i.com`(다른 인프라는 `do4ai.com`). 헷갈리기 쉬움.
  - Headlamp ingress에 오타 호스트 `heaplamp.do4ai.com` 공존.
  - 레지스트리: do4i·palcar는 Harbor, **papersens·passv·namanva는 AWS ECR** 사용(혼용). 서비스마다 다름.
  - passv는 **현재 prod가 EC2(docker compose) + S3/CloudFront**, K8s(`passv` 네임스페이스)로 **이전 중**. 가이드의 K8s 절차는 컷오버 후 기준.
  - 시크릿 실체는 **Infisical Operator**(InfisicalSecret CRD)다. 일부 README가 Sealed Secrets로 쓰여 있으나 그건 레거시.
- **참고(저장소 동기화 이슈)**: 위키 서브모듈의 gitops 스냅샷(`wiki/gitops/`)이 실제 `gitops` 레포보다 **구버전**이다(서브모듈에는 passv 앱, HPA, blackbox-exporter 등이 빠져 있음). 위키 가이드 본문은 최신 상태 기준으로 쓰여 있으므로, 서브모듈을 최신으로 갱신해 두는 것을 권장.

## 부록 B. 더 읽을거리(기존 위키 경로)
- 인프라 전반: `Guide/01. 인프라와 플랫폼/index.md`
- 서비스 운영: `Guide/02. 서비스 운영/index.md`
- 관측·SRE: `Guide/03. 관측과 SRE/index.md`
- gitops 레포 개요: `gitops/README.md`
